import React from "react";

function Header() {
  return (
    <div>
      <button>New Query</button>
      <button>Clear</button>
      <button>Run</button>
    </div>
  );
}

export default Header;
